# WorkManager 仕様書 v0.1.3

作成日：2026年9月9日  
対象：現在の実装（v0.1.3）。本書は実装内容を整理したもので、全機能の実機動作を保証する試験報告書ではありません。

## 1. 目的・構成

NAS上の案件Excelをブラウザで一覧表示・検索・編集し、Excelへ保存するWebアプリです。取引先と担当者はExcelマスターから生成したJSONを利用します。

- 利用環境：Windowsのローカルネットワーク、Apache、PHP 7.4対応
- 画面：HTML、CSS、JavaScript、Vue 3（Options API）
- Excel処理：ブラウザ側のSheetJS
- Vue 3はunpkgのCDN版、SheetJSはCDN版0.20.3を使用
- PHPはファイルの取得・保存とJSONの処理を担当し、Excelの内容は解析しません。
- Composer、PhpSpreadsheet、npm、ビルド環境、TypeScript、データベースは使用しません。

## 2. データの場所

### 案件データ

現在の`load.php`と`save.php`は、次の同一ファイルを指定しています。

```text
\\192.168.100.4\disk1\WorkManager\WorkManager.xlsx
```

以前のテスト用ファイル名ではなく、現在は`WorkManager.xlsx`です。変更する場合は両PHPの`$excelPath`を合わせます。

### マスターデータ

| 用途 | 初回の取得元（NASのWorkManagerフォルダー） | ローカル保存先 |
| --- | --- | --- |
| 取引先 | 取引先名マスタ.xlsx | data/customerMaster.json |
| 担当者 | 取引先担当者マスタ.xlsx | data/customerContactMaster.json |

「ローカル」はWebサーバー側のWorkManagerフォルダーを指します。ブラウザの端末内ストレージではありません。

## 3. 起動・読み込み

1. PHPが動作するHTTP URLから`index.html`を開きます。
2. 案件Excelと2つのマスターの読み込みを開始します。
3. ExcelはSheetJSで解析し、最初のワークシートを対象にします。
4. 読み込み中は「データをロード中です...」を表示します。
5. 読み込みが終わるまで一覧を非表示にし、保存テストを無効化します。

案件Excelの最初の取得行を見出し、以降をデータ行として扱います。表示用データは基本的にセルの書式付き文字列です。

マスターは個別に成功・失敗を判定します。一方が失敗しても他方の成功データは保持し、エラーを画面へ表示します。

## 4. マスターJSON

### 生成・再利用

- JSON取得が成功した場合、そのJSONを利用します。
- JSONが404（未作成）の場合だけ、元のExcelを取得してJSONを生成・保存します。
- JSONの形式不正、404以外の取得エラー、通信失敗はエラーとし、自動的にExcelへ切り替えません。
- Excelからの生成時に、ID未設定・空欄・空白だけの行を除外します。
- 同一IDは最初に出現した行を採用します。比較時はIDの前後の空白を除きます。
- 取引先は「取引先ID」、担当者は「担当者ID」を基準にします。
- 通常のJSON読み込み時は再度の重複除去・空ID除去を行いません。
- 取引先JSONの旧形式（二次元配列のrows）は読み込み時に現形式へ変換・保存します。

### データ形式

`rows`はExcelの見出しをキーとするオブジェクト配列です。Workbookや別のrecords配列は保存しません。以下の値は形式説明用の例です。

```json
{
  "sheetName": "customer",
  "rows": [
    {
      "取引先名": "サンプル株式会社",
      "取引先ID": "C-00000001",
      "ふりがな": "さんぷるかぶしきかいしゃ",
      "インデックス用イニシャル": "さんぷる",
      "検索用": "さ行"
    }
  ]
}
```

```json
{
  "sheetName": "customerContact",
  "rows": [
    {
      "取引先名": "サンプル株式会社",
      "担当者氏名": "山田 太郎",
      "担当者ID": "P-00000001"
    }
  ]
}
```

担当者JSONには実際のExcelにある部署・住所などの列も含まれます。上記例は一部の項目のみを示しています。

### 担当者JSONの分割保存

大容量JSONを一括POSTするとPHPの受信・メモリ制限に達するため、担当者JSONは分割して送信します。

- 1回あたり最大500件、レコード部分のUTF-8サイズ約1MB以下
- 1件だけで1MBを超える場合はエラー
- 送信識別子・書き込み位置・最終送信フラグを付けて順番に送信
- PHPは受信分だけを解析し、一時ファイルへ追記
- PHP側は受信サイズ上限1,100,000バイトと最大500件を検査
- 全件完了後に`customerContactMaster.json`へ名前を変更して公開
- コンソールに保存済み件数を表示

取引先JSONは一括送信し、一時ファイルへ保存した後に公開します。

## 5. 案件一覧・検索

- Excelの最終データ行から逆順に表示します。Excel内の保存順は変えません。
- 見出しを全ページに表示し、データ行を100件ずつ表示します。
- 「前へ」「次へ」、現在ページ・総ページ数・件数を表示します。
- 「取引先名」「案件名」の部分一致検索ができます。入力すると絞り込みます。
- 両方の検索欄に入力した場合はAND条件です。
- 検索時はNFKC正規化、前後空白除去、小文字化を行います。
- 「クリア」で両方の条件を解除します。検索条件を変えると1ページ目に戻ります。
- 該当件数と全件数を表示し、0件の場合は案内文を表示します。
- 検索は読み込み済みデータに対してブラウザ側で行います。
- 検索・ページ移動後も、元のExcel行番号を保持して編集対象を特定します。

## 6. 案件編集

一覧の行クリック、または行にフォーカスしてEnterを押すと編集画面を開きます。

項目は見出し名に含まれる文字から、以下の順に分類します。

| 分類 | 判定する文字 | 操作 |
| --- | --- | --- |
| 取引先・担当者情報 | 取引先、顧客、担当者、住所 | 編集可能 |
| 請求情報 | 請求、見積、金額、税、入金 | 編集可能 |
| 案件情報 | 案件、制作、管理番号、件名 | 編集可能 |
| その他 | 上記以外 | 通常のテキスト表示・編集不可 |

画面の表示順は案件情報、取引先・担当者情報、請求情報、その他です。プルダウンと日付欄以外の編集可能項目はテキスト入力です。

### 取引先・担当者の選択とID連動

- 取引先名：`customerMaster.rows`の取引先名から選択します。
- 担当者氏名：選択した取引先名と一致する`customerContactMaster.rows`の担当者だけを表示します。
- 各選択肢の同名表示は重複を除去します。
- 取引先名変更時：取引先IDを反映し、担当者氏名と担当者IDをクリアします。
- 担当者氏名変更時：取引先名と氏名が一致する担当者IDを反映します。
- IDの対応検索では、最初に一致したマスターレコードを使用します。
- マスターにない既存の取引先名も現在値として表示します。候補外の担当者も現在値として表示します。
- 取引先未選択・マスター未取得の場合は該当プルダウンを無効にします。

編集画面では、次の列名を短く表示します。Excelの見出しそのものは変更しません。

| Excelの列名 | 画面表示 |
| --- | --- |
| 取引先担当者住所参照::担当者ID | 担当者ID |
| 取引先名マスタ::取引先ID | 取引先ID |

### 日付

- 請求日・請求予定日：ブラウザ標準のカレンダーピッカーで選択します。
- 入力部品へはYYYY-MM-DD形式を渡し、選択後の編集値はYYYY/M/D形式にします。
- ピッカーの実際の表示形式はブラウザ・地域設定に従います。
- データ入力日・集計日：編集画面でYYYY/M/D形式のテキスト表示にします。
- `7/28/26`は`2026/7/28`として解釈します。2桁年は00〜29を2000年代、30〜99を1900年代として扱います。
- 既存値の表示変換だけでは元の編集値を変更しません。

### 編集の終了

「変更を保存」で保存し、「一覧へ戻る」「キャンセル」で未保存の編集内容を破棄して一覧へ戻ります。破棄前の確認ダイアログはありません。

## 7. 案件Excelへの保存

1. 編集中の行をSheetJSのワークシートへ反映します。
2. Workbook全体をxlsxバイナリへ再生成します。
3. `save.php`へ一括POSTします。
4. PHPが同じNASフォルダーの`WorkManager.xlsx.tmp`へ書き込み、サイズを確認します。
5. 一時ファイルから対象Excelへコピーして上書きし、一時ファイルを削除します。
6. 成功時は一覧のメモリ上の行も更新し、「変更を保存しました」と表示します。

保存対象ファイルの存在・書き込み権限、受信データが空でないこと、先頭がPKであることを検査します。PK確認は完全なxlsx検証ではありません。

編集保存の失敗時はエラーを表示し、Workbookの対象行を編集前の表示値で書き戻します。これはNASのバックアップ復元や、セルの数式・書式を完全復元する処理ではありません。

## 8. 保存テスト

一覧の「保存テスト」は実際の案件Excelの末尾にテスト行を追加する機能です。

値はTEST、テスト案件、操作日時、10000、未請求を列数に合わせて設定します。列が多い場合は残りを空文字とします。成功後は1ページ目へ移動しますが、検索条件は保持されるため、条件によっては追加行が表示されません。

## 9. ファイルの役割

| ファイル | 役割 |
| --- | --- |
| index.html | 一覧・検索・編集画面、CDN読み込み |
| css/style.css | レイアウト・入力欄・表・スマートフォン向け表示 |
| js/app.js | Excel解析、マスター生成、検索、編集、保存、バージョン出力 |
| php/load.php | 案件Excelを取得 |
| php/save.php | 案件Excelを上書き保存 |
| php/load-customer-master.php | 取引先名マスターExcelを取得 |
| php/save-customer-master.php | 取引先JSONを保存 |
| php/load-customer-contact-master.php | 担当者マスターExcelを取得 |
| php/save-customer-contact-master.php | 担当者JSONを分割受信・保存 |
| data/customerMaster.json | 取引先マスター |
| data/customerContactMaster.json | 担当者マスター |
| CHANGELOG.md | バージョンごとの変更履歴 |

## 10. 運用条件・現在の制限

- PHPを実行するユーザーにNASの読み書き権限とローカルdataフォルダーへの書き込み権限が必要です。
- `file://`ではなくPHPが実行されるHTTP URLから開きます。CDNを取得できるネット接続が必要です。
- マスターExcelを更新しても既存JSONは自動更新されません。再生成ボタンは未実装です。再生成する場合は対象JSONを退避してから画面を読み込み直します。
- 分割保存は担当者JSONのみです。案件Excelは全量送信、取引先JSONも一括送信します。
- 初回の大きなExcel解析はブラウザ側で全体を扱うため時間とメモリを要します。
- 中断した担当者JSONの一時ファイルを自動削除・自動再開する機能はありません。
- 同名の取引先、同一取引先内の同姓同名担当者をID別に選び分けるUIはありません。
- ID欄は自動連動しますが、現在はテキスト入力として手動編集も可能です。
- その他欄は画面で編集不可ですが、保存時には選択行全体を再出力します。数式・セル型・高度なExcel機能の完全保持は保証しません。
- 案件保存のコピー置換は原子的な置換ではありません。自動バックアップ、複数利用者の競合検知、アプリ全体の排他制御は未実装です。
- ユーザー認証、セッション管理、正式な新規案件登録・削除、税額自動計算、請求書発行は未実装です。

## 11. バージョン管理

現在の起動ログは`console.log('ver 0.1.3')`です。

今後のアプリ更新では起動ログとCHANGELOG.mdを同時に更新します。1回の更新を1バージョンとして扱います。

- パッチ：不具合修正・表示調整（例：0.1.3 → 0.1.4）
- マイナー：機能追加（例：0.1.4 → 0.2.0）
- メジャー：互換性に影響する大きな変更（例：1.0.0 → 2.0.0）
- 開発中は0.x.xで進め、正式運用開始時は1.0.0を目安とします。

本書はv0.1.3時点の記録として作成し、アプリのバージョン自体は変更していません。
