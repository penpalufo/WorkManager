# WorkManager 第1段階・簡易仕様書

## 1. 目的

WorkManagerは、仕事の案件や請求状況などを管理するためのWebアプリです。

第1段階では、NAS上のExcelファイルをブラウザに表示し、テストデータを1件追加して同じExcelファイルへ保存できるところまでを実装しています。

## 2. 動作環境

- Windowsのローカルネットワーク内で使用
- Webサーバー：Apache
- PHP：7.4.12
- フロントエンド：HTML、CSS、JavaScript
- Vue 3：CDN版、Options API
- SheetJS：CDN版

Composer、PhpSpreadsheet、npm、Vite、Webpack、TypeScriptなどは使用しません。

## 3. 対象Excelファイル

読み込み先と保存先には、次の同一ファイルを使用します。

```text
\\192.168.100.4\disk1\WorkManager\WorkManager---test10.xlsx
```

Apacheを実行するWindowsユーザーには、このファイルへの読み取り権限と書き込み権限が必要です。

## 4. ファイル構成

```text
WorkManager/
├─ index.html
├─ css/
│  └─ style.css
├─ js/
│  └─ app.js
└─ php/
   ├─ load-customer-master.php
   ├─ load-customer-contact-master.php
   ├─ load.php
   └─ save.php
```

各ファイルの役割は次のとおりです。

- `index.html`：画面構成とVue 3・SheetJSのCDN読み込み
- `css/style.css`：表、ボタン、メッセージなどの表示調整
- `js/app.js`：Excelの取得、解析、表への表示、テスト行の追加と保存
- `php/load-customer-master.php`：NAS上の取引先名マスターを取得する
- `php/load-customer-contact-master.php`：NAS上の取引先担当者マスターを取得する
- `php/load.php`：NAS上のExcelファイルをバイナリデータとして返す
- `php/save.php`：ブラウザから送られたExcelデータをNASへ保存する

## 5. マスターファイル

画面起動時に、次の2つのExcelファイルもNASから読み込みます。

```text
\\192.168.100.4\disk1\WorkManager\取引先名マスタ.xlsx
\\192.168.100.4\disk1\WorkManager\取引先担当者マスタ.xlsx
```

ブラウザ側では、それぞれ`customerMaster`と`customerContactMaster`という独立したVueオブジェクトに格納します。各オブジェクトには、SheetJSのWorkbook、先頭シート名、行配列、見出しをプロパティ名にしたレコード配列が含まれます。

## 6. 読み込み処理

1. ブラウザから`index.html`を開きます。
2. Vueの起動時に`php/load.php`を呼び出します。
3. `load.php`がNAS上のExcelファイルを読み込み、そのままブラウザへ返します。
4. ブラウザ側のSheetJSがExcelを解析します。
5. 最初のワークシートをJavaScript配列へ変換します。
6. VueがExcelの内容をHTMLの表として表示します。

表の1行目は見出しとして固定し、2行目以降のデータをExcelの最終行から逆順に100件ずつ表示します。「前へ」「次へ」ボタンでページを切り替えられ、現在のページ、総ページ数、全件数も表示します。Excelファイル内の行順自体は変更しません。

一覧の案件行をクリックすると案件編集画面へ切り替わります。Excelの見出し名を使って案件情報、取引先・担当者情報、請求情報、その他の項目に分けて表示し、各値を変更できます。「変更を保存」を押すと、選択した行を更新してNAS上のExcelへ保存します。

PHP側ではExcelの内容を解析しません。

## 7. 保存テスト処理

画面の「保存テスト」ボタンを押すと、最初のワークシートの末尾へテストデータを1行追加します。

主な追加内容は次のとおりです。

- `TEST`
- `テスト案件`
- 保存操作を行った日時
- `10000`
- `未請求`

追加する値は既存Excelの列数に合わせ、余った列には空文字を設定します。

SheetJSでExcelファイルを再生成し、バイナリデータを`php/save.php`へPOSTします。保存成功時は画面に「保存しました」と表示します。

## 8. 保存時の安全対策

`save.php`では、受信データが空でないことと、xlsx形式に必要な先頭データを最低限確認します。

受信したデータは、最初にNAS上の一時ファイルへ全量を書き込みます。書き込んだサイズを確認した後、対象Excelファイルへコピーして上書きし、一時ファイルを削除します。

## 9. エラー処理

次のような場合は、画面にエラーメッセージを表示します。

- NAS上に対象ファイルがない
- ファイルを読み取れない、または書き込めない
- NASへ接続できない
- Excelデータの取得や解析に失敗した
- 保存用データが空、またはxlsx形式ではない
- PHPへの送信やNASへの保存に失敗した

保存に失敗した場合は、メモリ上に追加したテスト行を残さないため、Excelファイルを読み直します。

## 10. 動作確認方法

1. WorkManagerフォルダーをApacheで公開します。
2. ブラウザから`http://localhost/.../index.html`へアクセスします。
3. Excelの内容が表に表示されることを確認します。
4. 対象ExcelをExcelアプリで開いている場合は閉じます。
5. 「保存テスト」を押します。
6. 「保存しました」と表示されることを確認します。
7. NAS上の`WorkManager---test10.xlsx`を開き直し、末尾にテスト行が追加されたことを確認します。

`index.html`を`file://`で直接開くのではなく、必ずApache経由で開いてください。また、Vue 3とSheetJSをCDNから取得するため、初回表示時にはインターネットへ接続できる環境が必要です。

## 11. 現段階で実装しない機能

- ユーザー認証、セッション管理、複数ユーザー対応
- 画面からのセル編集、検索、ソート、削除
- 請求管理、入金管理
- データベース
- アプリ全体の排他ロック
- ComposerおよびPhpSpreadsheet
- npmおよびビルド環境

これらは基本的な読み込みと保存の動作を確認した後、必要に応じて段階的に追加します。
